const itemType = {
  COFFEE: 'coffee',
  JUICE: 'juice',
  FOOD: 'food',
};

const orderItemState = {
  ORDERED: 'ordered',
  PENDING: 'pending',
  COMPLETED: 'completed',
  CANCELED: 'canceled',
};

module.exports = { itemType, orderItemState };
